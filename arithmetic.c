#include <stdio.h>
int main()
{
    int X = 40;
    int Y = 30;
    int add = X + Y;
    printf("CHECKING ADDITION FUNCTION\n");

    if (add = 70)
    {
        printf("The above satisfies ADDITION\n");
    }

    else if (add != 70)
    {
        printf("The above does not satisfy ADDITION\n");
    }
    else
        printf("Try again\n");
}